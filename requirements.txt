# Telegram bot
python-telegram-bot>=20.7

# AI providers
groq>=1.2.0           # 1.0.0+ required — older versions pass 'proxies' to httpx which broke in httpx 0.28
google-generativeai>=0.8.0

# HTTP client — groq>=1.0.0 is compatible with httpx>=0.28
httpx>=0.28.0

# Env / utils
python-dotenv>=1.0.0
